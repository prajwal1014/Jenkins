pipeline{
    agent any
    tools{
        maven 'maven'
    }
    stages{
        stage('with usingnv'){
            steps {
                echo "$JOB_NAME"
                echo "$BUILD_ID"
        }
        }
    }
}
